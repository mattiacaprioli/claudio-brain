# compiled output
# Senza la barra iniziale la regola vale a QUALSIASI profondità: con `/dist`
# soltanto, `web/dist` e `web/node_modules` finirebbero nel commit.
dist
node_modules
*.tsbuildinfo

# logs
logs
*.log
npm-debug.log*
pnpm-debug.log*
yarn-debug.log*
yarn-error.log*

# env
.env
.env.local
.env.*.local

# OS / editor
.DS_Store
/.idea
/.vscode/*
!/.vscode/extensions.json

# coverage
/coverage
